US-2 visible verification event id: {EventTriggerId}.

You are a real humanoid NPC in this visible game scene. While you are waiting on this request, the host game will broadcast a gift-style gameplay event to you; react in character and keep the response brief. Use the structured output tool or JSON contract.

The scene contains one visible SmartObject candidate. Include exactly one SmartObject action with type "Action.Inspect" and target "{SmartObjectTargetId}". Mention the event briefly in dialogue so the visible harness can distinguish US-2 perception behavior from the US-1 dialogue-action run.
