# Cleanup Inventory Ledger

Change: `cleanup-redundant-code-and-artifacts`

Source snapshot:
- `git status --short` captured on 2026-05-19 before any cleanup action.
- OpenSpec change artifacts exist under `openspec/changes/cleanup-redundant-code-and-artifacts/`.
- `openspec/` is gitignored in this repo, so this ledger stays as working evidence unless explicitly force-added through an approved handoff path.

## Inventory Columns

| Path / Symbol | Category | Git State | Ownership / Source | Evidence | Recommended Action | Approval Required | Risk | Required Verification |
|---|---|---|---|---|---|---|---|---|
| `Intermediate/` | Generated artifact | ignored | Unreal build output cache | Large ignored output directory from current scan in design/proposal context | Preserve as ignored generated output | No | Low | None beyond ignore policy |
| `Saved/` | Generated artifact / run state | ignored | Unreal runtime/editor output | Large ignored output directory from current scan | Preserve as ignored generated output; classify only if evidence is needed later | No | Low | None beyond ignore policy |
| `Binaries/` | Generated artifact | ignored | Unreal build output | Large ignored output directory from current scan | Preserve as ignored generated output | No | Low | None beyond ignore policy |
| `Plugins/AINpc/Binaries/` | Generated artifact | ignored | Plugin build output | Large plugin-local generated output from current scan | Preserve as ignored generated output | No | Low | None beyond ignore policy |
| `Plugins/AINpc/Intermediate/` | Generated artifact | ignored | Plugin build output | Large plugin-local generated output from current scan | Preserve as ignored generated output | No | Low | None beyond ignore policy |
| `DerivedDataCache/` | Generated artifact | ignored | Unreal derived-data cache | Design context names it as ignored generated cache output | Ignore; preserve in place during this worker pass because no cleanup/delete approval is in scope | No | Low | None beyond ignore policy |
| `TestResults/` | Generated artifact / evidence cache | untracked | Test and packaging output | Current `git status --short` shows untracked `TestResults/`; current scan shows many packaged logs/manifests | Preserve for now; later tasks may classify per subpath as evidence or cleanup candidate | Yes | Medium | Record before/after status before any removal |
| `Build/Windows/FileOpenOrder/` | Generated artifact | untracked | Unreal editor/build output | `Build/Windows/FileOpenOrder/EditorOpenOrder.log` appears in current scan and `git status --short` shows untracked `Build/` | Preserve for now; likely generated evidence, not source | Yes | Medium | Record before/after status before any removal |
| `docs/ralph/` | Mixed docs / run-state / artifacts | untracked except `docs/ralph/test-matrix.json` tracked | Workflow state material | Current scan says `docs/ralph/state/` is untracked and `docs/ralph/image/` ignored | Keep mixed-surface classification separate; do not treat as a single delete bucket | Yes | Medium | Separate tracked docs from run-state before any action |
| `docs/ralph/state/` | Run-state / artifact | untracked | Workflow output | Current scan in design doc | Preserve as evidence until later ownership decision | Yes | Medium | Preserve path-group status snapshot |
| `docs/ralph/image/` | Generated visual artifact cache | ignored | Ralph workflow image output | `git status --ignored -- docs/ralph` shows ignored `docs/ralph/image/` | Ignore; preserve in place during this worker pass because images may be visual evidence and deletion is out of scope | Yes | Medium | Summarize or explicitly approve before any removal |
| `Plugins/AINpc/*.log` | Generated log output | ignored | Plugin-local build/debug logs | Directory listing found ignored plugin-root logs; `.gitignore` already has `/Plugins/AINpc/*.log` | Ignore; preserve in place during this worker pass and summarize only if a later task needs the proof | Yes | Low | Capture pointer before deleting any log used as evidence |
| `Plugins/AINpc/Source/AINpcCore/Private/Components/AINpcProviderConfigResolver.h` | Duplicate source surface | modified | Private implementation header | Current diff shows modified private header; design doc lists public/private duplicate pair | Candidate for ownership review with public twin; do not change by inventory alone | Yes | High | Build/include impact check after owner decision |
| `Plugins/AINpc/Source/AINpcCore/Public/Components/AINpcProviderConfigResolver.h` | Duplicate source surface | untracked | Public header twin | Current `git status --short` shows untracked public twin; design doc lists it as duplicate pair | Candidate for owner selection and include cleanup | Yes | High | Build/include impact check after owner decision |
| `Plugins/AINpc/Source/AINpcCore/Public/AINpcComponent.h` | Forwarding / duplicate header surface | modified | Public runtime header | Design doc flags duplicate header name against `Public/Components/AINpcComponent.h` | Review as forwarding header; keep or remove only after include impact is checked | Yes | High | Build/include impact check |
| `Plugins/AINpc/Source/AINpcCore/Public/Components/AINpcComponent.h` | Public runtime header | modified | Canonical public component header candidate | Current worktree modification and design doc duplicate-header note | Keep as primary surface unless later evidence shows otherwise | Yes | High | Build/include impact check if any ownership changes |
| `Plugins/AINpc/Source/AINpcCore/Private/LLM/StructuredOutputPromptLibrary.cpp` | Duplicate source surface | untracked | Shared prompt/schema helper | Current `git status --short` shows new structured-output helper | Candidate source for schema consolidation evidence; keep until owner is chosen | Yes | Medium | Serialize request-body comparison before any move |
| `Plugins/AINpc/Source/AINpcCore/Private/LLM/StructuredOutputPromptLibrary.h` | Duplicate source surface | untracked | Shared prompt/schema helper | Current `git status --short` shows new helper header | Candidate source for schema consolidation evidence; keep until owner is chosen | Yes | Medium | Serialize request-body comparison before any move |
| `Plugins/AINpc/Source/AINpcCore/Public/Components/AINpcComponent.h: ForTest APIs` | Public test seam | modified | Runtime-facing public API | `rg` shows many `ForTest` methods and counters in public header | Inventory only for now; placement review required before narrowing | Yes | High | Update affected automation tests if moved or narrowed |
| `Plugins/AINpc/Source/AINpcCore/Public/LLM/OpenAIProvider.h: BuildRequestBodyForTest / timing hooks` | Public test seam | modified | Provider runtime header | `rg` shows public `ForTest` methods and static timing hooks | Keep in ledger for placement review; do not remove blindly | Yes | High | Contract tests and request-body comparison |
| `Plugins/AINpc/Source/AINpcCore/Public/LLM/AnthropicProvider.h: BuildRequestBodyForTest / timing hooks` | Public test seam | modified | Provider runtime header | `rg` shows public `ForTest` methods and static timing hooks | Keep in ledger for placement review; do not remove blindly | Yes | High | Contract tests and request-body comparison |
| `Plugins/AINpc/Source/AINpcCore/Public/LLM/LLMConcurrencyManager.h: ResetForTest / slot setters` | Public test seam | modified | Concurrency manager header | `rg` shows public test hooks on runtime-facing manager | Inventory only; keep or narrow based on later placement decision | Yes | Medium | Fast tests plus any affected automation |
| `Plugins/AINpc/Source/AINpcCore/Public/SmartObjectBridge/SmartObjectBridgeContext.h: AddTrackedClaimForTest` | Public test seam | modified | SmartObject bridge runtime header | `rg` shows public test hooks on bridge context | Inventory only; likely belongs in test-only boundary review | Yes | Medium | Affected automation coverage if moved |
| `Plugins/AINpc/Source/AINpcCore/Private/Components/AINpcDialogueLifecycleHandler.h: SetDispatchBypassForTest` | Runtime test seam / bypass hook | modified | Private component handler | `rg` shows bypass hook in lifecycle handler | Keep as seam candidate; no expansion or rewrite now | Yes | High | Runtime behavior verification if touched later |
| `Plugins/AINpc/Source/AINpcCore/Private/Components/AINpcSmartObjectPromptHandler.h: SetTargetsOverrideForTest / ClearTargetsOverrideForTest` | Runtime test seam | modified | Private prompt handler | `rg` shows SmartObject prompt test override hooks | Preserve for inventory; later placement review required | Yes | High | Affected automation and runtime verification if narrowed |
| `Config/AINpcPromptTemplate.txt` | Runtime text | untracked | Tracked config/template intended for prompt body | `rg` shows world-framing prompt text and output examples | Keep as tracked runtime text asset; classify as valid template ownership | No | Medium | Packaging/load verification only if moved |
| `Config/AINpcPromptFragments.txt` | Runtime text | untracked | Tracked config/template fragment asset | File exists in worktree; referenced by tests in `AINpcReliabilityAutomationTests.cpp` | Keep as tracked template fragment evidence | No | Medium | Packaging/load verification only if moved |
| `Config/AINpcStructuredOutputJsonInstruction.txt` | Runtime text | untracked | Tracked config/template asset | `rg` shows strict JSON instruction text | Keep as tracked output-contract asset unless later ownership review says otherwise | No | Medium | Packaging/load verification only if moved |
| `Config/AINpcStructuredOutputStrictJsonInstruction.txt` | Runtime text | untracked | Tracked config/template asset | `rg` shows strict JSON instruction text | Keep as tracked output-contract asset unless later ownership review says otherwise | No | Medium | Packaging/load verification only if moved |
| `Config/AINpcStructuredOutputToolDescription.txt` | Runtime text | untracked | Tracked config/template asset | File exists in worktree; tied to structured-output prompt plumbing | Keep as tracked template/policy asset | No | Medium | Packaging/load verification only if moved |
| `Config/AINpcLocalProvider.example.json` | Runtime text / example config | untracked | Example provider config | File exists in worktree and is likely sample-only | Keep as example evidence until later policy says otherwise | Yes | Low | Only if later moved into tracked config policy |
| `Plugins/AINpc/Source/AINpcCore/Private/Components/AINpcDialogueFallbackHandler.cpp` | Runtime text candidate | modified | Fallback handler | `rg` shows timeout fallback log text and template-response wording | Inventory as runtime text source for later placement review | Yes | Medium | Provider/request contract checks if altered |
| `Plugins/AINpc/Source/AINpcCore/Private/LLM/LLMProviderChain.cpp` | Runtime text candidate | modified | Provider chain orchestration | `rg` shows `Action.DefaultTalk` and degradation message wording | Inventory as default/fallback text owner candidate | Yes | Medium | Contract checks if text moves |
| `Plugins/AINpc/Source/AINpcCore/Private/LLMResponseParser.cpp` | Runtime text candidate | modified | Response parser | `rg` shows `Action.DefaultTalk` and parser error wording | Inventory as fallback/default text source | Yes | Medium | Parse/contract tests if behavior changes |
| `Plugins/AINpc/Source/AINpcCore/Private/SmartObjectBridge/AINpcSmartObjectRuntimeExecutor.cpp` | Runtime text candidate | untracked | SmartObject runtime executor | `rg` shows `Action.DefaultTalk` handling | Inventory as runtime default-action handling candidate | Yes | Medium | Affected automation if changed |
| `Plugins/AINpc/Source/AINpcCore/Private/Test/AINpcTestGameMode.cpp: InitialPrompt` | Runtime prompt / visual harness input | untracked | Real-provider visual verification harness | QA scan found the long `StartDialogue` prompt literal used to drive a real SmartObject action in the visible harness | Move the prompt body to a tracked config/template file and keep only placeholder assembly in C++; preserve effective content and fail clearly if the template cannot load | No | Medium | Focused literal scan, packaging/load staging check, fast build/test if feasible |
| `Plugins/AINpc/Source/AINpcCore/Private/StateTree/Tasks/StateTreeTask_ExecuteSmartObject.cpp` | Runtime text candidate / test seam | modified | StateTree task | `rg` shows `ValidateSmartObjectActionForTest` and whitelist logic | Inventory as mixed seam/behavior source; do not move without task split | Yes | Medium | Affected automation + build checks |
| `Plugins/AINpc/Source/AINpcCore/Private/StateTree/Tasks/StateTreeTask_LLMQuery.cpp` | Test seam / runtime flow | modified | StateTree task | `rg` shows `ApplyExitStateCleanupForTest` and waiting-state helper | Inventory only; later review for placement | Yes | Medium | Affected automation if narrowed |
| `Plugins/AINpc/Source/AINpcCore/Private/Test/` | Validation harness | untracked | New harness source tree | Current `git status --short` shows untracked harness tree | Preserve as replacement harness surface until replacement chain is fully evidenced | Yes | High | Harness replacement evidence and runtime verification |
| `Plugins/AINpc/Source/AINpcCore/Public/Test/` | Validation harness | untracked | New harness public test tree | Current `git status --short` shows untracked harness tree | Preserve as replacement harness surface until replacement chain is fully evidenced | Yes | High | Harness replacement evidence and runtime verification |
| `Source/VerifierHost.Target.cs` | Validation harness replacement | untracked | Host target for editor/game build path | Current `git status --short` shows untracked host target | Keep in harness group; do not delete or rename in inventory phase | Yes | High | Build path verification |
| `VerifierHostAutomation.uproject` | Validation harness replacement | untracked | New automation host project | Current `git status --short` shows untracked project; design/proposal names it | Preserve as harness evidence; do not treat as disposable artifact | Yes | High | Build/run verification chain |
| `Content/Maps/AINpcTestMap.umap` | Validation harness replacement | untracked | Test map asset | Mentioned in design/proposal and pipeline logs via `/Game/Maps/AINpcTestMap` | Preserve as visual/runtime verification surface | Yes | High | Visible editor/game verification only |
| `Config/DefaultEngine.ini` | Validation harness wiring | modified | Project config | Design/proposal names it as part of replacement harness chain | Keep in harness replacement group; do not clean as artifact | Yes | High | Build/run verification chain |
| `Plugins/AINpc/AINpc.uplugin` | Validation harness wiring | modified | Plugin descriptor | Design/proposal names it as harness replacement wiring | Keep in harness replacement group; no cleanup action yet | Yes | High | Build/run verification chain |
| `Plugins/AINpc/Source/AINpcCore/AINpcCore.Build.cs` | Validation harness wiring | modified | Core module rules | Design/proposal names it as harness replacement wiring | Keep in harness replacement group; no cleanup action yet | Yes | High | Build/run verification chain |
| `Plugins/AINpc/Source/AINpcEditor/AINpcEditor.Build.cs` | Validation harness wiring | modified | Editor module rules | Design/proposal names it as harness replacement wiring | Keep in harness replacement group; no cleanup action yet | Yes | High | Build/run verification chain |
| `VerifierHost.uproject` | Validation harness wiring | modified | Host project file | Design/proposal names it as harness replacement wiring | Keep in harness replacement group; no cleanup action yet | Yes | High | Build/run verification chain |
| `scripts/dev/build-editor.ps1` | Validation harness wiring | modified | Local automation script | Design/proposal names it as harness replacement wiring | Keep in harness replacement group; no cleanup action yet | Yes | Medium | Build/run verification chain |
| `Plugins/AINpc/Source/AINpcCore/Private/Tests/AINpcGauntletTestController.cpp` | Validation harness churn | deleted | Old Gauntlet controller | `git status --short` shows deleted file; design/proposal call it out | Inventory only; do not restore/delete further without approval chain | Yes | High | Replacement path and before/after status snapshot |
| `Plugins/AINpc/Source/AINpcCore/Private/Tests/AINpcGauntletTestController.h` | Validation harness churn | deleted | Old Gauntlet controller | `git status --short` shows deleted file; design/proposal call it out | Inventory only; do not restore/delete further without approval chain | Yes | High | Replacement path and before/after status snapshot |
| `.gitignore` / `openspec/` | Process boundary | modified / ignored | Repo policy | `git status --short` shows modified `.gitignore`; repo has ignored `openspec/` | Record that OpenSpec files remain ignored unless explicitly force-added via approved handoff | Yes | Medium | Confirm handoff path before expecting commit visibility |

## Notes

- The current worktree is already dirty and includes user/parallel-agent changes. This ledger treats every entry as evidence-only until an explicit task authorizes a cleanup action.
- `docs/ralph/test-matrix.json` is the only tracked `docs/ralph` file noted in the design context; the `docs/ralph/state/` subtree is untracked workflow state and must remain separate from tracked docs in later tasks.
- `TestResults/` and `Build/Windows/FileOpenOrder/` are useful evidence-bearing outputs right now; keep them classified as generated artifacts, not as immediate delete targets.
- Any later move touching runtime text or harness surfaces needs the corresponding verification in the tasks file, not just a ledger note.

## Task 2 Artifact Hygiene Decisions

Decision date: 2026-05-19

This worker pass is non-destructive. No file was deleted, moved, archived, or restored. The decisions below are policy and evidence routing only.

| Path / Group | Decision | Evidence | Risk / Rationale | Next Verification Before Any Destructive Action |
|---|---|---|---|---|
| `Intermediate/` | `ignore` | Already ignored by `/Intermediate/`; design context reports about 17,003 files and 19,314.10 MB | Generated Unreal output; tracking would be noise. Keep local copy untouched because deletion is outside this worker scope. | None for ignore policy; before deletion, capture `git status --short --ignored -- Intermediate` and user approval if needed. |
| `Saved/` | `ignore` | Already ignored by `/Saved/`; design context reports about 1,939 files and 3,035.81 MB | Generated editor/runtime state that can include logs. Ignore policy is correct, but logs may carry evidence. | Summarize relevant logs before any cleanup that removes evidence. |
| `Binaries/` | `ignore` | Already ignored by `/Binaries/`; design context reports about 18 files and 2,496.98 MB | Generated build output; not source. | Build can regenerate; record status before deletion if later requested. |
| `Plugins/AINpc/Binaries/` | `ignore` | Covered by `Plugins/*/Binaries/`; design context reports about 11 files and 293.18 MB | Generated plugin binaries; not source. | Build can regenerate; record status before deletion if later requested. |
| `Plugins/AINpc/Intermediate/` | `ignore` | Covered by `Plugins/*/Intermediate/`; design context reports about 241 files and 48.34 MB | Generated plugin intermediate output; not source. | Build can regenerate; record status before deletion if later requested. |
| `DerivedDataCache/` | `ignore` | Already ignored by `/DerivedDataCache/`; design context names it as generated cache output | Generated cache; no source value. | None unless a later performance/build investigation needs a pointer before cleanup. |
| `Plugins/AINpc/*.log` | `summarize` | Already ignored by `/Plugins/AINpc/*.log`; sampled plugin-root logs include build/debug logs | Logs are generated but can be the only proof of a past build/debug run. Do not track the bulky logs; keep or summarize before deletion. | Add a small pointer for any log used as acceptance evidence before removing it. |
| `TestResults/` | `summarize` | Now ignored by `/TestResults/`; pointer file records `index.json` 5/5 mock-provider success and `PipelineLogs/ci_20260518-124417.log` 80/80 automation pass | Generated and bulky, but currently evidence-bearing. It must stay local until a later cleanup task captures enough proof or gets explicit approval. | Preserve `verification/artifact-evidence-pointers.md`; before deletion, capture before/after status and explicit approval. |
| `Build/Windows/FileOpenOrder/` | `ignore` | Now ignored by `/Build/Windows/FileOpenOrder/`; pointer file records `EditorOpenOrder.log` and `CookerOpenOrder.log` generated on 2026-05-19 | Generated build/editor ordering output. Low source risk, but it explains recent `VerifierHostAutomation.uproject` runs. | Pointer exists; before deletion, capture before/after status and explicit approval. |
| `docs/ralph/test-matrix.json` | `preserve` | `git ls-files -- docs/ralph` shows only this tracked file | Tracked documentation/configuration for the Ralph workflow; must not be hidden by broad ignore rules. | Normal docs review only. |
| `docs/ralph/state/` | `summarize` | Now ignored by `/docs/ralph/state/`; pointer file records `verdict.json` with `storyId: RW-1.1`, `runId: 20260518-012838-211`, and `RALPH_TEST_RESULT: PASS` | Untracked run state may be unique workflow evidence. Ignore future generated state, but do not delete this state in this pass. | Preserve pointer; before deletion, capture before/after status and explicit approval. |
| `docs/ralph/image/` | `ignore` | Already ignored by `/docs/ralph/image/`; `git status --ignored` shows ignored image output | Generated visual artifacts may still be useful evidence. Keep separate from tracked docs and run state. | Summarize image evidence or get explicit approval before removal. |
| `docs/ralph/stories/` | `investigate` | Directory exists but `git status --short --untracked-files=all -- docs/ralph` did not report files in this pass | No cleanup decision needed without files/evidence. Avoid broad `docs/ralph/` ignore that would hide future docs. | Re-scan path if it becomes non-empty. |
| `docs/ralph/tests/` | `investigate` | Directory exists with `temp/`, but no tracked source was found by `git ls-files -- docs/ralph` | Could be workflow scratch, but current task only named `state/` and `image/`; avoid accidental policy overreach. | Re-scan path if later cleanup targets it. |

## Task 2.2 Ignore/Cleanup Policy

- Added exact generated/local-output ignore entries for `/TestResults/`, `/Build/Windows/FileOpenOrder/`, and `/docs/ralph/state/`.
- No broad ignore was added for `docs/ralph/`, `Build/`, `Config/*.txt`, `Content/Maps/*.umap`, `VerifierHostAutomation.uproject`, or other real source/config/docs/visual automation assets.
- Existing ignore policy is sufficient for `/Intermediate/`, `/Saved/`, `/Binaries/`, `/DerivedDataCache/`, `Plugins/*/Binaries/`, `Plugins/*/Intermediate/`, `/docs/ralph/image/`, `/Plugins/AINpc/*.log`, local provider config, local agent/tool state, and `/.artifacts/`.
- Because `openspec/` remains ignored, this change-local ledger and pointer file require the established handoff path if they must become commit-visible.

## Task 2.3 Evidence Pointers

- Added `openspec/changes/cleanup-redundant-code-and-artifacts/verification/artifact-evidence-pointers.md`.
- The pointer records compact evidence for `TestResults/`, `Build/Windows/FileOpenOrder/`, `docs/ralph/state/`, and sampled plugin-root logs without copying large generated outputs.
- The pointer explicitly says generated automation evidence is not final visible NPC behavior acceptance.

## Task 2.5 `docs/ralph/` Mixed Path Decision

- `docs/ralph/test-matrix.json`: tracked documentation/workflow asset; decision `preserve`.
- `docs/ralph/state/`: untracked run state; decision `summarize` and now exact-ignore future local state while preserving the current contents in place.
- `docs/ralph/image/`: ignored generated images; decision `ignore` while preserving current images in place because they may be visual evidence.
- `docs/ralph/stories/` and `docs/ralph/tests/`: decision `investigate`; no broad ignore or cleanup action in this pass.
- The path must never be treated as one cleanup bucket.

## Task 2.4/2.6 Cleanup Approval and Execution

- Approval: user explicitly requested cleanup closeout and acceptance on 2026-05-20 with `收啊，验收也做`.
- Scope authorized for this closeout: generated/cache/evidence outputs already inventoried as generated or run-state material: `Intermediate/`, `Saved/`, `Binaries/`, `Plugins/AINpc/Binaries/`, `Plugins/AINpc/Intermediate/`, `DerivedDataCache/`, `TestResults/`, `Build/Windows/FileOpenOrder/`, `docs/ralph/state/`, `docs/ralph/image/`, and ignored plugin-root logs matching `Plugins/AINpc/*.log`.
- Scope excluded from deletion: source, config/template files, `.uproject` files, `Content/` test map assets, `VerifierHostAutomation.uproject`, `Source/VerifierHost*.Target.cs`, OpenSpec change artifacts, pointer evidence under `openspec/changes/cleanup-redundant-code-and-artifacts/verification/`, tracked `docs/ralph/test-matrix.json`, and any unclassified scripts or harness files.
- Before cleanup status snapshot:
  - `!! Binaries/`
  - `!! Build/Windows/FileOpenOrder/`
  - `!! DerivedDataCache/`
  - `!! Intermediate/`
  - `!! Plugins/AINpc/Binaries/`
  - `!! Plugins/AINpc/G?:UE5AI-NPC-Pluginbuild_us1t12_fix.log`
  - `!! Plugins/AINpc/Intermediate/`
  - `!! Plugins/AINpc/build_us2at4.log`
  - `!! Saved/`
  - `!! TestResults/`
  - `!! docs/ralph/image/`
  - `!! docs/ralph/state/`
- Evidence preservation before deletion: `verification/artifact-evidence-pointers.md` records compact pointers for `TestResults/`, `Build/Windows/FileOpenOrder/`, `docs/ralph/state/`, and sampled plugin-root logs. These generated outputs are not final visible NPC behavior acceptance evidence.
- Cleanup action: removed only the approved generated/run-state path group after verifying resolved paths stayed under `G:\UE5\AI-NPC-Plugin`.
- After cleanup status snapshot for the same path group: `git status --short --ignored -- Intermediate Saved Binaries Plugins/AINpc/Binaries Plugins/AINpc/Intermediate DerivedDataCache TestResults Build/Windows/FileOpenOrder docs/ralph/state docs/ralph/image Plugins/AINpc/*.log` produced no output.
- Preservation check after cleanup: `Content/Maps/AINpcTestMap.umap`, `VerifierHostAutomation.uproject`, `Config/AINpcVisualHarnessInitialPrompt.txt`, `openspec/changes/cleanup-redundant-code-and-artifacts/verification/artifact-evidence-pointers.md`, and `Plugins/AINpc/Source/AINpcCore/Private/Test/AINpcTestGameMode.cpp` still exist. `git status --short` for key harness/source files still shows the expected untracked source/config/harness files rather than deletion.

## Task 3.1 Action Record

- Final owner: `Plugins/AINpc/Source/AINpcCore/Private/Components/AINpcProviderConfigResolver.h`.
- Evidence: live resolver includes are private-module callers (`AINpcComponent.cpp`, `AINpcDialogueRequestBuilder.cpp`, `AINpcProviderConfigResolver.cpp`, and the private test game mode). The first three use same-directory private includes; the private test game mode still uses `Components/AINpcProviderConfigResolver.h`, and the generated dependency file resolves it to `Source\AINpcCore\Private\Components\AINpcProviderConfigResolver.h`. No public runtime header or non-core module includes the resolver.
- Action: updated same-directory private component includes to `#include "AINpcProviderConfigResolver.h"` and removed the untracked public twin `Plugins/AINpc/Source/AINpcCore/Public/Components/AINpcProviderConfigResolver.h`.
- Rationale: the resolver constructs providers and applies component request defaults for private runtime orchestration; exporting it would expose provider bootstrap internals and the public twin also contained unimplemented test override declarations.
- Scope check: no provider behavior, config parsing, prompt text, fallback text, runtime defaults, or engine source was changed for this task.
- Verification: `git diff --check -- <task 3.1/3.2 touched files>` passed on 2026-05-19; `pwsh ./scripts/dev/test-fast.ps1` passed on 2026-05-19 with `PASS: BaseURL contract verified`, `PASS: SSE contract verified`, and `PASS: fast AI NPC checks completed`; `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict` passed on 2026-05-19.

## Task 3.2 Action Record

- Decision: keep `Plugins/AINpc/Source/AINpcCore/Public/AINpcComponent.h` as an existing public API forwarding header.
- Evidence: the plugin's internal call sites now use the canonical `Components/AINpcComponent.h` path; the forwarding header remains only as a public include entry point for downstream code that may include `AINpcComponent.h` from the module public root.
- Action: updated the two AINpcEditor node includes from `#include "AINpcComponent.h"` to `#include "Components/AINpcComponent.h"`.
- Rationale: deleting the public-root component include would be a public API break, while keeping the existing one-line forwarding header does not add or expand a legacy compatibility path.
- Scope check: no Blueprint API, component declarations, test seams, runtime behavior, or generated artifacts were changed for this task.
- Verification: no internal `#include "AINpcComponent.h"` call sites remain after the editor node include updates; `git diff --check -- <task 3.1/3.2 touched files>` passed on 2026-05-19; `pwsh ./scripts/dev/test-fast.ps1` passed on 2026-05-19; `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict` passed on 2026-05-19.

## Task 3.3 Action Record

- Final action: consolidated Anthropic structured-output `input_schema` onto `StructuredOutputSchemaHelpers::BuildStructuredOutputInputSchema()`, which forwards to the same schema owner used by OpenAI and Local request bodies.
- Touched files: `Plugins/AINpc/Source/AINpcCore/Private/LLM/StructuredOutputSchemaHelpers.h`, `Plugins/AINpc/Source/AINpcCore/Private/LLM/AnthropicProvider.cpp`, `Plugins/AINpc/Source/AINpcCore/Private/LLM/LocalProvider.cpp`, `Plugins/AINpc/Source/AINpcCore/Public/LLM/LocalProvider.h`, and `Plugins/AINpc/Source/AINpcCore/Private/Tests/AINpcReliabilityAutomationTests.cpp`.
- Out-of-scope cleanup: removed the attempted Anthropic streaming parser, endpoint auto-`/v1`, empty-model guards, default-model changes, and prompt-library behavior changes from the task 3.3 diff before acceptance.
- Request-body evidence: see `openspec/changes/cleanup-redundant-code-and-artifacts/verification/structured-output-request-body-diff.txt`.
- Verification: `git diff --check -- <task 3.3 touched files>` passed on 2026-05-19; `pwsh ./scripts/dev/test-fast.ps1` passed on 2026-05-19 with `PASS: BaseURL contract verified`, `PASS: SSE contract verified`, and `PASS: fast AI NPC checks completed`; `openspec validate cleanup-redundant-code-and-artifacts --strict` passed on 2026-05-19.
- Engine-source check: `git diff --name-only` and `git status --short --untracked-files=all` inside `G:\UE5\AI-NPC-Plugin` showed no `Engine/` or Unreal Engine source paths owned by this plugin cleanup. The shared engine workspace must still be checked separately at final closeout; see the Task 6 final engine-source boundary note.
- Historical follow-up at this point: closeout tasks 6.1-6.4 were still pending until the later final cleanup closeout because later tasks touched additional files and needed a fresh final verification pass.

## Task 3.4 Action Record

- Canonical owner: `AINpc::Actions::DefaultTalkActionType` in `Plugins/AINpc/Source/AINpcCore/Public/LLM/LLMResponseParser.h`, next to the public `FNpcAction` / parsed-response types that carry action symbols across parser, provider-chain fallback, state-handler filtering, SmartObject execution, and automation tests.
- Evidence: current `rg` found `Action.DefaultTalk` duplicated in `LLMResponseParser.cpp`, `LLMProviderChain.cpp`, `AINpcComponentStateHandler.cpp`, `AINpcSmartObjectRuntimeExecutor.cpp`, and parser assertions in `AINpcReliabilityAutomationTests.cpp`; after this pass, source references use `AINpc::Actions::DefaultTalkActionType` and the only remaining literal is the canonical definition.
- Action: replaced the existing hard-coded default-talk action literal with the canonical constant and added direct `LLM/LLMResponseParser.h` includes where runtime files use the constant.
- Scope check: no string content, compare case-sensitivity, trim behavior, parser tier ordering, fallback response text, provider behavior, prompt text, config behavior, SmartObject whitelist behavior, generated artifacts, or engine source was changed.
- Behavior note: this is byte-equivalent for the action string (`Action.DefaultTalk`) and is intentionally not a runtime-text relocation task; broader prompt/default/fallback text placement remains under later task group 4.
- QA correction (2026-05-19): main QA found that `LLMResponseParser.cpp` still carried out-of-scope Anthropic `tool_use` parser expansion from the 3.4/3.5 pass (`ParseStructuredResponseObject`, `TryParseAnthropicToolUseInputObject`, and `ParseAnthropicMessages` handling for `tool_use` / `input_json`), which conflicted with the 3.3 record that the attempted Anthropic parser was removed before acceptance.
- Correction action: removed that Anthropic parser expansion and left `LLMResponseParser.cpp` with only the `AINpc::Actions::DefaultTalkActionType` replacement for this task group. The 3.3 structured-output request-body schema/helper consolidation and the 3.4 canonical constant owner remain in place; no task checkbox was changed.
- Correction verification: `rg -n "tool_use|input_json|TryParseAnthropicToolUseInputObject|ParseStructuredResponseObject" Plugins/AINpc/Source/AINpcCore/Private/LLMResponseParser.cpp` returned no matches; the `DefaultTalkActionType` scan showed the canonical owner plus runtime/test references and no non-definition hard-coded `TEXT("Action.DefaultTalk")`; `git diff --check -- <QA correction touched files>`, `pwsh ./scripts/dev/test-fast.ps1`, and `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict` passed on 2026-05-19.

## Task 3.5 Action Record

- Compatibility gate result: no compatibility wrapper, old include-path shim, old field/path fallback, dual-read/write behavior, silent migration, or legacy behavior preservation path was added, preserved, or expanded in this worker pass.
- Existing fallback/loose parsing code was not modified for compatibility purposes; no new fallback field name, path, provider endpoint behavior, prompt text, or migration branch was introduced.
- No user approval was required because the pass did not identify a need to add or keep any new compatibility mechanism. If later duplicate cleanup requires one, the task remains to stop and request explicit user approval before implementation.

## Task 4.1/4.2 Action Record

- Reviewed public runtime-facing `ForTest`/counter seam groups in `UAINpcComponent`, `FLLMConcurrencyManager`, provider headers, SmartObject bridge context, and StateTree task headers.
- Final action for `UAINpcComponent`: narrowed ordinary `ForTest` APIs and dynamic delegate counter fields from editor/non-shipping exposure to `defined(WITH_DEV_AUTOMATION_TESTS) && WITH_DEV_AUTOMATION_TESTS`. The two dynamic delegate handler `UFUNCTION`s (`HandleDialogueResponseDynamicForTest` and `HandleDialogueErrorDynamicForTest`) remain unguarded as reflected functions because UHT rejects `UFUNCTION` declarations inside ordinary preprocessor blocks; their counter side effects remain guarded to dev automation builds only.
- Final action for `FLLMConcurrencyManager`: narrowed `ResetForTest`, `SetActiveDialogueSlotsForTest`, and `SetActiveMemorySlotsForTest` from editor/non-shipping exposure to `defined(WITH_DEV_AUTOMATION_TESTS) && WITH_DEV_AUTOMATION_TESTS`.
- Test update: changed `Plugins/AINpc/Source/AINpcCore/Private/Tests/LLMConcurrencyAutomationTests.cpp` from `WITH_EDITOR` to the same `WITH_DEV_AUTOMATION_TESTS` guard so direct coverage follows the narrowed concurrency seam. `AINpcReliabilityAutomationTests.cpp` stays `WITH_EDITOR` because it also exercises provider and StateTree seams that currently remain editor-only.
- Provider seam decision: kept OpenAI, Anthropic, and Local `BuildRequestBodyForTest`, request delay, cancel-gate, and base-url helpers under existing `WITH_EDITOR` guards. Current tests instantiate provider classes directly from editor automation and the request-body helpers are needed for contract/schema coverage; moving them requires a larger private test adapter split and is not necessary for 4.1.
- SmartObject bridge decision: kept the public bridge context test hooks under existing `WITH_DEV_AUTOMATION_TESTS`, which already matches the preferred automation-only boundary.
- StateTree seam decision: kept `ValidateSmartObjectActionForTest`, `ApplyExitStateCleanupForTest`, and `EvaluateWaitingStateForTest` under existing `WITH_EDITOR`; these are editor automation seams tied to StateTree task testing, and broadening or relocating them would be outside this narrow 4.1/4.2 pass.
- Scope check: no runtime behavior, provider behavior, prompt text, fallback text, parser behavior, public Blueprint API, generated artifact, or engine source was changed. No compatibility wrappers, old include-path shims, old field/path fallbacks, dual-read/write behavior, or silent migration were added.
- Verification: first `pwsh ./scripts/dev/test-fast.ps1` attempt failed with UHT errors because `UFUNCTION` declarations were inside `WITH_DEV_AUTOMATION_TESTS`; the implementation was adjusted to keep those reflected declarations unguarded while guarding their counter behavior. After that, `git diff --check -- <task 4.1/4.2 touched files>` passed, `pwsh ./scripts/dev/test-fast.ps1` passed with editor build plus `PASS: BaseURL contract verified`, `PASS: SSE contract verified`, and `PASS: fast AI NPC checks completed`, and `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict` passed on 2026-05-19.
- Main QA verification (2026-05-19): independently re-read the 4.1/4.2 diff and public seam guards, scanned public/private source for `ForTest`/counter/bypass seams, confirmed the parser `tool_use` correction remains zero-hit, confirmed no engine source paths appear in `git diff --name-only`, and reran `git diff --check -- <task 4.1/4.2 touched files>`, `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict`, and `pwsh ./scripts/dev/test-fast.ps1`; all passed, with only existing LF/CRLF warnings from Git.

## Task 4.3 Action Record

- Reviewed old and new validation harness surfaces together. Current old Gauntlet controller files are deleted in the worktree: `Plugins/AINpc/Source/AINpcCore/Private/Tests/AINpcGauntletTestController.cpp` and `.h` both return `False` from `Test-Path` and appear as `D` in `git status --short`.
- Replacement harness surfaces are present but mixed tracked states: `VerifierHostAutomation.uproject`, `Content/Maps/AINpcTestMap.umap`, and `Source/VerifierHost.Target.cs` are untracked; `VerifierHost.uproject`, `Source/VerifierHost/VerifierHost.Build.cs`, `Config/DefaultEngine.ini`, `Plugins/AINpc/AINpc.uplugin`, `Plugins/AINpc/Source/AINpcCore/AINpcCore.Build.cs`, `Plugins/AINpc/Source/AINpcEditor/AINpcEditor.Build.cs`, and `scripts/dev/build-editor.ps1` are modified wiring; `Source/VerifierHostEditor.Target.cs` and `Source/VerifierHost/VerifierHost.cpp` are existing host surfaces.
- Replacement path evidence: `VerifierHostAutomation.uproject` is a minimal AI NPC automation host enabling `AINpc` and disabling `Design2UMG`; `VerifierHost.uproject` enables `AINpc`, required gameplay/editor dependencies, and `Gauntlet`; `scripts/dev/build-editor.ps1` builds `VerifierHostEditor` against `VerifierHost.uproject`; `Source/VerifierHost.Target.cs` and `Source/VerifierHostEditor.Target.cs` build the `VerifierHost` module.
- Verification evidence available now: `pwsh ./scripts/dev/test-fast.ps1` passed on 2026-05-19 with editor build plus `PASS: BaseURL contract verified`, `PASS: SSE contract verified`, and `PASS: fast AI NPC checks completed`.
- Boundary decision: do not delete, restore, or accept removal of the old Gauntlet controller files in this cleanup pass. The replacement chain is inventoried and build-checked, but final acceptance for any player-visible NPC behavior still requires the real visible editor/game verification path from task 6.5. Headless, commandlet, mock, bypass, or injected-response evidence does not count as final gameplay acceptance.

## Task 4.4/4.5/4.6 Action Record

- Final action: moved remaining structured-output prompt/output-contract literals in `Plugins/AINpc/Source/AINpcCore/Private/LLM/LocalProvider.cpp` and `Plugins/AINpc/Source/AINpcCore/Private/LLM/AnthropicProvider.cpp` to the existing `AINpc::StructuredOutputPrompts` owner in `StructuredOutputPromptLibrary`.
- Local provider mapping: tool description now uses `GetToolDescription()`, JSON mode system instruction now uses `GetJsonInstruction()`, and strict/no-json-mode instruction now uses `GetStrictJsonInstruction()`.
- Anthropic provider mapping: tool description now uses `GetToolDescription()`, and strict fallback instruction now uses `GetStrictJsonInstruction()`.
- Request contract scope: field names, tool schema ownership, `tools`, `tool_choice`, `messages`, `response_format`, and message insertion order were not changed by this task; only the source of the existing text values changed.
- Compatibility gate result: no old-path compatibility, dual-read behavior, silent migration, legacy fallback, fallback prompt text, or config-path shim was added. No user approval was required because this pass did not require compatibility.
- Packaging/load evidence: `Plugins/AINpc/Source/AINpcCore/AINpcCore.Build.cs` already stages `Config/AINpcStructuredOutputJsonInstruction.txt`, `Config/AINpcStructuredOutputStrictJsonInstruction.txt`, and `Config/AINpcStructuredOutputToolDescription.txt` as `RuntimeDependencies` with `StagedFileType.NonUFS`; `StructuredOutputPromptLibrary.cpp` already loads those exact filenames through `FPaths::ProjectConfigDir()`.
- Contract verification coverage: existing editor automation request-body tests use `BuildRequestBodyForTest` and include `AINpc.Core.Reliability.OpenAIProviderRequestsStructuredOutputContract` plus `AINpc.Core.Reliability.StructuredOutputSchemaParity` for OpenAI, Local, and Anthropic request bodies. No separate request-body artifact generator was found in this pass.
- Required scan: `rg -n "Return structured NPC dialogue|You must respond with valid JSON matching this schema|Do not include any text outside" Plugins/AINpc/Source/AINpcCore/Private/LLM/LocalProvider.cpp Plugins/AINpc/Source/AINpcCore/Private/LLM/AnthropicProvider.cpp` returned no matches after the change.
- Verification: `git diff --check -- <task 4.4/4.5/4.6 touched files>` passed on 2026-05-19; `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict` passed on 2026-05-19; `pwsh ./scripts/dev/verify-baseurl-contract.ps1` and `pwsh ./scripts/dev/verify-sse-contract.ps1` both passed on 2026-05-19. `pwsh ./scripts/dev/test-fast.ps1` was attempted but blocked by Unreal Live Coding: `Unable to build while Live Coding is active. Exit the editor and game, or press Ctrl+Alt+F11 if iterating on code in the editor or game`; no full fast-test result is claimed for this pass.
- QA reopen (2026-05-20): main QA found one remaining hard-coded LLM input in `Plugins/AINpc/Source/AINpcCore/Private/Test/AINpcTestGameMode.cpp`, where `StartInitialDialogue()` builds the real-provider visual harness prompt body directly in C++. This text is not a provider/system prompt and is part of the visual verification harness, but it is still an LLM input template and therefore belongs in tracked config/template ownership under the repo prompt-text rule.
- Reopen action: tasks 4.4 and 4.6 were reopened. The remaining implementation is to move that visual-harness prompt body into a tracked config/template file, stage/load it without legacy fallback behavior, preserve effective behavior, and rerun focused scans plus packaging/load/build verification. At that point tasks 2.4 and 2.6 were unrelated destructive-cleanup gates; they were later completed after explicit cleanup approval, as recorded in `Task 2.4/2.6 Cleanup Approval and Execution`.
- Final reopen action (2026-05-20): moved the visual harness initial LLM input body from `Plugins/AINpc/Source/AINpcCore/Private/Test/AINpcTestGameMode.cpp` to tracked template `Config/AINpcVisualHarnessInitialPrompt.txt`. The template preserves the existing prompt wording and uses `{SmartObjectTargetId}` as the explicit SmartObject target-id placeholder.
- Load/staging behavior: `AINpcTestGameMode.cpp` now loads `AINpcVisualHarnessInitialPrompt.txt` from `FPaths::ProjectConfigDir()`, verifies the file is non-empty and contains `{SmartObjectTargetId}`, replaces that placeholder with the selected runtime target id, and calls the existing `RecordFailure(...)` harness failure path if the template is missing or malformed. No old literal fallback, old-path compatibility, dual-read behavior, silent migration, or legacy behavior preservation branch was added.
- Packaging evidence: `Plugins/AINpc/Source/AINpcCore/AINpcCore.Build.cs` now stages `Config/AINpcVisualHarnessInitialPrompt.txt` with `RuntimeDependencies.Add(..., StagedFileType.NonUFS)`.
- Focused scan evidence: `rg -n "A bright interaction pillar is next to you\. Inspect it now and use the exact SmartObject target id|AINpcVisualHarnessInitialPrompt|SmartObjectTargetId|Failed to load visual harness|legacy|fallback|compat|dual-read|migration" Plugins\AINpc\Source\AINpcCore\Private\Test\AINpcTestGameMode.cpp Config\AINpcVisualHarnessInitialPrompt.txt Plugins\AINpc\Source\AINpcCore\AINpcCore.Build.cs -S` showed the old long prompt body only in `Config/AINpcVisualHarnessInitialPrompt.txt`, showed the NonUFS staging line in Build.cs, and found no fallback/compatibility/migration wording in the touched implementation files.
- Behavior/acceptance boundary: this pass changes template ownership and missing-template failure handling only. It does not intentionally change the effective prompt content, SmartObject target selection, provider behavior, parser behavior, NPC movement, dialogue state flow, UI, generated artifacts, or engine source. No visible gameplay acceptance is claimed for this ownership cleanup.
- Verification update: `git diff --check -- Plugins/AINpc/Source/AINpcCore/Private/Test/AINpcTestGameMode.cpp Plugins/AINpc/Source/AINpcCore/AINpcCore.Build.cs` passed with only existing LF/CRLF warnings; `git diff --name-only | rg "(^|/)Engine/|UnrealEngine|Engine\\Source"` returned no matches for engine source changes. Final strict OpenSpec validation and fast-test results are recorded in the Task 6 closeout status below.

## Task 5.1/5.2/5.3 Hotspot Triage Record

- Current hotspot scan (nonblank source lines, 2026-05-19) found: `AINpcReliabilityAutomationTests.cpp` 2180, `SmartObjectBridgeContext.cpp` 633, `NpcDialogueBubbleWidgetAutomationTests.cpp` 592, `OpenAIProvider.cpp` 586, `AnthropicProvider.cpp` 514, `LLMResponseParser.cpp` 462, `AINpcComponent.cpp` 433, `LocalProvider.cpp` 406, `AINpcDialogueLifecycleHandler.cpp` 375, and `AINpcTestGameMode.cpp` 370.
- `AINpcReliabilityAutomationTests.cpp`: maps to multiple inventory groups rather than one redundant owner: provider request contracts, parser tier coverage, StateTree cleanup seams, SmartObject bridge cleanup, fallback/retry, event routing, and prompt/template coverage. Recommended action is keep during this change; future extraction should be by behavior cluster only when it preserves the same tests and reduces review risk, not as a cosmetic split.
- Provider hotspots (`OpenAIProvider.cpp`, `AnthropicProvider.cpp`, `LocalProvider.cpp`): mapped to duplicate schema ownership and runtime text placement. Actions already taken in this change are schema owner consolidation and structured-output prompt/text owner consolidation. No further provider split is authorized without a fresh inventory item proving real duplication or misplaced responsibility.
- `SmartObjectBridgeContext.cpp`: mapped to SmartObject runtime bridge ownership and public test seam inventory. It is large but currently owns a cohesive runtime bridge lifecycle; do not split or delete because it supports player-visible SmartObject verification and claim cleanup.
- `NpcDialogueBubbleWidgetAutomationTests.cpp`: mapped to UI/streaming automation coverage. It is test-heavy but protects player-visible dialogue bubble behavior; no cleanup action in this change because there is no replacement verification path or duplicate owner.
- `LLMResponseParser.cpp`: mapped to parser behavior and `Action.DefaultTalk` canonicalization. The accepted cleanup is the canonical action constant only; do not add provider-specific parser behavior or compatibility branches as part of cleanup.
- `AINpcComponent.cpp` and `AINpcDialogueLifecycleHandler.cpp`: mapped to component facade / dialogue lifecycle ownership and public test seam narrowing. This change narrowed automation-only seams; further extraction is allowed only for a concrete inventory item that removes real duplication or wrong-layer logic.
- `AINpcTestGameMode.cpp`: mapped to the visual verification harness. It must be kept unless a replacement path and visible editor/game evidence are recorded. It is not generated clutter.
- Decision for 5.2: no additional code split is performed in this pass. The only hotspot edits already made were tied to concrete inventory items: provider schema/text ownership, parser action constant ownership, include ownership, and automation seam boundaries.
- Decision for 5.3: visual automation and gameplay smoke-test harness code is preserved. No harness file is deleted, restored, moved, or accepted as replaced by this task group; task 6.5 is the final visible-gameplay acceptance gate when behavior changes.

## Task 6 Verification and Closeout Status

- 6.1 `git diff --check`: full `git diff --check` passed on 2026-05-19. Git printed existing LF/CRLF working-copy warnings, but no whitespace errors were reported.
- 6.2 `test-fast`: the first attempt after the 4.4/4.6 provider prompt-source change was blocked by Unreal Live Coding. After the editor process was no longer present, `pwsh ./scripts/dev/test-fast.ps1` passed on 2026-05-19 with `VerifierHostEditor` build plus `PASS: BaseURL contract verified`, `PASS: SSE contract verified`, and `PASS: fast AI NPC checks completed`. After the shared-engine BuildId repair below, main QA reran `pwsh ./scripts/dev/test-fast.ps1`; it passed again with `Target is up to date`, `PASS: BaseURL contract verified`, `PASS: SSE contract verified`, and `PASS: fast AI NPC checks completed`.
- 6.3 provider/request checks: `pwsh ./scripts/dev/verify-baseurl-contract.ps1` passed and `pwsh ./scripts/dev/verify-sse-contract.ps1` passed on 2026-05-19. Existing editor automation covers request-body/schema behavior through `BuildRequestBodyForTest`.
- 6.3 Gauntlet/load repair evidence: the first direct `UnrealEditor-Cmd.exe` automation attempt failed before test execution because the shared engine had mismatched `.modules` BuildIds: `UnrealEditor.version` and AI-NPC outputs expected the current editor BuildId, while `Gauntlet`, `SmartObjects`, `GameplayAbilities`, `TargetingSystem`, and `WorldConditions` manifests were stale. Main QA rebuilt `G:\UE5\FPSRoguelike\FPSRoguelike.uproject` with the correct UBT command-line form, `-EnablePlugin=Gauntlet+SmartObjects+GameplayAbilities+TargetingSystem+WorldConditions`, then rebuilt `VerifierHostEditor`. Verified manifests afterward: `G:\UE5\UnrealEngine\Engine\Binaries\Win64\UnrealEditor.version`, `Plugins/AINpc/Binaries/Win64/UnrealEditor.modules`, `Binaries/Win64/UnrealEditor.modules`, and all five engine plugin `.modules` files had BuildId `7f7f5434-b42a-43ee-94f3-240ae2e4cdb0`.
- 6.3 automation report evidence: `UnrealEditor-Cmd.exe G:\UE5\AI-NPC-Plugin\VerifierHost.uproject -ExecCmds="Automation RunTests AINpc.Core.Reliability.StructuredOutputSchemaParity;Quit" -TestExit="Automation Test Queue Empty" -nop4 -nosplash -unattended -ReportExportPath=...\verification\automation-structured-output` completed with exit code 0. Report `verification/automation-structured-output/index.json` recorded `succeeded=1`, `failed=0`, test `AINpc.Core.Reliability.StructuredOutputSchemaParity`, state `Success`.
- 6.3 automation report evidence: `UnrealEditor-Cmd.exe G:\UE5\AI-NPC-Plugin\VerifierHost.uproject -ExecCmds="Automation RunTests AINpc.Core.Reliability.OpenAIProviderRequestsStructuredOutputContract;Quit" -TestExit="Automation Test Queue Empty" -nop4 -nosplash -unattended -ReportExportPath=...\verification\automation-openai-request-contract` completed with exit code 0. Report `verification/automation-openai-request-contract/index.json` recorded `succeeded=1`, `failed=0`, test `AINpc.Core.Reliability.OpenAIProviderRequestsStructuredOutputContract`, state `Success`.
- 6.3 command-line note: a combined `-ExecCmds` attempt with two `Automation RunTests` commands in one string only queued the first test and logged `Unknown Automation command` for the second command. The accepted evidence is therefore the two separate automation runs above.
- 6.4 engine-source guard: `git diff --name-only | rg "(^|/)Engine/|UnrealEngine|Engine\\Source"` returned no matches inside `G:\UE5\AI-NPC-Plugin` on 2026-05-19; exit code 1 is expected for zero matches. Final closeout also checked the shared engine workspace separately, as recorded below.
- 6.5 visual verification boundary: this cleanup changed code ownership, test seam guards, and prompt text source paths, but did not intentionally change player-visible NPC dialogue content, prompt content, parser behavior, provider output interpretation, memory/state transitions, UI behavior, or gameplay harness behavior. No visible editor/game acceptance is claimed for this cleanup pass. The headless/editor automation above is contract evidence only. If a later pass changes actual NPC behavior, prompt content, state flow, memory, UI, or provider interpretation, the real visible editor/game chain remains mandatory.
- 6.6 interim ledger status before destructive cleanup approval: every candidate group discovered in this change had a final action recorded as keep, preserve evidence, consolidate owner, narrow test seam, move misplaced prompt text, or leave pending explicit approval. At that interim point, no generated artifact deletion, movement, or archival had been performed, and tasks 2.4 and 2.6 stayed open because destructive cleanup of dirty/untracked/ignored/deleted paths still required explicit approval and before/after `git status --short` evidence. Those cleanup gates were later completed after approval, as recorded above and in the final closeout entries below.
- 2026-05-20 reopen closeout update: tasks 4.4 and 4.6 were re-completed for the residual visual-harness prompt ownership issue before tasks 2.4 and 2.6 received their separate cleanup approval. Main QA independently reran the focused prompt scan, NonUFS staging scan, `git diff --check -- <reopen touched files>`, `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict`, `pwsh ./scripts/dev/test-fast.ps1`, and the engine-source guard. The prompt body remained only in `Config/AINpcVisualHarnessInitialPrompt.txt`; the new template was staged as `NonUFS`; OpenSpec strict validation passed; fast test passed with BaseURL and SSE contract checks; the engine-source guard returned no matches, with only existing LF/CRLF Git warnings.
- 2026-05-20 visible QA failure: main QA launched exactly one visible game window with `G:\UE5\UnrealEngine\Engine\Binaries\Win64\UnrealEditor.exe G:\UE5\AI-NPC-Plugin\VerifierHost.uproject /Game/Maps/AINpcTestMap -game -log -windowed -ResX=1280 -ResY=720 -NoSplash`. The process exited 0, but `Saved\Logs\VerifierHost.log` recorded `VISUAL QA FAILED: Dialogue fell back instead of producing a real provider response. FailureReason=Failed to parse response as JSON Fallback=I need a moment to think. Could you ask me again?`.
- 2026-05-20 root cause: Anthropic structured requests were sent with `stream=true`; the returned Server-Sent Events body reached `FAnthropicProvider::CompleteRequest`, which still called the complete-body `ParseAnthropicMessages` path and rejected the SSE stream as non-JSON. This is provider output interpretation and player-visible dialogue behavior, so task 6.5 is reopened until a new visible run passes.
- 2026-05-20 implementation repair: added Anthropic Messages `tool_use.input` parsing and internal Anthropic SSE aggregation for `content_block_start`, `content_block_delta` with `text_delta` / `input_json_delta`, and `content_block_stop`. `FLLMResponseParser::ParseAnthropicMessages` now preserves the existing complete-body path and, only when the body is not JSON, rebuilds the streamed content blocks into the same Anthropic Messages shape before parsing. No prompt wording, fallback text, legacy compatibility path, dual-read behavior, mock provider, bypass, parser public API, or engine source was added.
- 2026-05-20 focused verification after repair: `git diff --check -- Plugins/AINpc/Source/AINpcCore/Private/LLMResponseParser.cpp Plugins/AINpc/Source/AINpcCore/Public/LLM/LLMResponseParser.h Plugins/AINpc/Source/AINpcCore/Private/LLM/AnthropicProvider.cpp Plugins/AINpc/Source/AINpcCore/Private/Tests/AINpcReliabilityAutomationTests.cpp` passed with only existing LF/CRLF warnings; `pwsh ./scripts/dev/test-fast.ps1` passed with `VerifierHostEditor` build plus BaseURL and SSE contract checks; `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict` passed before task 6.5 was reopened for visible QA; focused editor automation `AINpc.Core.Parser.AnthropicStreamingToolUseStructuredOutput` passed with report `verification/automation-anthropic-streaming-tool-use/index.json` showing `succeeded=1`, `failed=0`; the engine-source guard returned no matches. These are not final visible acceptance.
- 2026-05-20 subagent correction after main QA review: main QA found the first repair wired `TryBuildAnthropicMessageBodyFromEventStream(...)` into `ParseOpenAIChatCompletion` instead of `ParseAnthropicMessages`, so both the real Anthropic path and the focused test still returned `Failed to parse response as JSON`. Corrected ownership by removing the Anthropic SSE rebuild branch from the OpenAI parser and invoking it only from `FLLMResponseParser::ParseAnthropicMessages` before that function returns `Failed to parse response as JSON`.
- 2026-05-20 corrected verification before final visible QA: after rebuilding with `pwsh ./scripts/dev/test-fast.ps1`, reran `UnrealEditor-Cmd.exe G:\UE5\AI-NPC-Plugin\VerifierHost.uproject -ExecCmds="Automation RunTests AINpc.Core.Parser.AnthropicStreamingToolUseStructuredOutput;Quit" -TestExit="Automation Test Queue Empty" -nop4 -nosplash -unattended -ReportExportPath=G:\UE5\AI-NPC-Plugin\openspec\changes\cleanup-redundant-code-and-artifacts\verification\automation-anthropic-streaming-tool-use-subagent`; report `index.json` shows `succeeded=1`, `failed=0`, `state=Success`, `errors=0`. Then `git diff --check -- <touched files>`, `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict`, and `pwsh ./scripts/dev/test-fast.ps1` all passed; the AI-NPC repo engine-source guard returned no matches. This was supporting evidence only; task 6.5 was completed by the later visible-window run recorded below.
- 2026-05-20 visible QA endpoint root cause update: main QA independently probed the configured Anthropic proxy without printing any API key and found `POST https://code.newcli.com/claude/aws/messages` returned 404, while `POST https://code.newcli.com/claude/aws/v1/messages` returned 200 JSON. The code appended `/messages` directly to any configured Anthropic base URL, so repo-local config `https://code.newcli.com/claude/aws` constructed the wrong endpoint and the parser then reported `Failed to parse response as JSON` on the 404 body.
- 2026-05-20 endpoint normalization repair: `FAnthropicProvider` now resolves the final Messages endpoint with a provider-owned rule: base URLs ending in `/messages` are used as-is, base URLs ending in `/v1` append `/messages`, and other base URLs append `/v1/messages`. This is current endpoint normalization for the Anthropic provider, not a domain-specific `code.newcli.com` branch, legacy compatibility path, dual-read, retry, migration, or fallback.
- 2026-05-20 endpoint verification before final visible QA: replaced the old log-only `AINpc.Anthropic.EndpointConstruction` test with real assertions for `/v1`, no-`/v1`, already-`/messages`, and per-request override base URLs. Focused automation reports show `AINpc.Anthropic.EndpointConstruction`, `AINpc.Core.Parser.AnthropicStreamingToolUseStructuredOutput`, and `AINpc.Core.Reliability.AnthropicStructuredOutputDisablesStreamingRequest` each passed with `succeeded=1`, `failed=0`, `state=Success`, `errors=0`. `git diff --check -- <touched files>`, `npx @fission-ai/openspec validate cleanup-redundant-code-and-artifacts --strict`, `pwsh ./scripts/dev/test-fast.ps1`, and the AI-NPC repo engine-source guard all passed. This was supporting evidence only; task 6.5 was completed by the later visible-window run recorded below.
- 2026-05-20 final visible QA pass: main QA confirmed no existing `UnrealEditor` process, then launched exactly one visible game window with `G:\UE5\UnrealEngine\Engine\Binaries\Win64\UnrealEditor.exe G:\UE5\AI-NPC-Plugin\VerifierHost.uproject /Game/Maps/AINpcTestMap -game -log -windowed -ResX=1280 -ResY=720 -NoSplash`. The process exited with code 0 and wrote `G:\UE5\AI-NPC-Plugin\Saved\Logs\VerifierHost.log`.
- 2026-05-20 final visible QA evidence: the log shows `Dialogue session started through the real provider chain`, `Anthropic endpoint constructed: https://code.newcli.com/claude/aws/v1/messages`, a real NPC response, `SmartObject action verified: inspect -> {B3FAE565-4BB5-8E20-2B28-C8B7655DAF2C}:0. NPC is moving to the claimed slot.`, `=== NPC Action Visual QA VERIFIED WITH REAL PROVIDER ===`, and `VISUAL QA VERIFIED: action target {B3FAE565-4BB5-8E20-2B28-C8B7655DAF2C}:0 claimed/used and NPC reached the claimed slot.` No `VISUAL QA FAILED`, fallback, mock provider, bypass, `UnrealEditor-Cmd`, `-unattended`, or NullRHI evidence is used as final acceptance.
- 2026-05-20 final closeout gate: after the visible pass, task 6.5 is complete. Focused automation, `openspec validate cleanup-redundant-code-and-artifacts --strict`, `git diff --check -- <touched files>`, `pwsh ./scripts/dev/test-fast.ps1`, and the engine-source guard had all passed before this final visible run. The final visible run is the acceptance evidence for player-visible NPC behavior; headless automation remains supporting evidence only.
- 2026-05-20 final evidence handoff policy: `openspec/` is ignored by `.gitignore`, and `git check-ignore -v openspec\changes\cleanup-redundant-code-and-artifacts\tasks.md` reports `.gitignore:34:/openspec/`. The OpenSpec change directory and its verification reports must therefore be either force-added explicitly for the closeout commit or exported as a separate handoff bundle; ordinary `git add .` will not capture them.
- 2026-05-20 final staging include list for this change: include the source, config/template, map, host, script, docs, and OpenSpec evidence files that are part of the accepted diff, including `Config/AINpcPromptTemplate.txt`, `Config/AINpcPromptFragments.txt`, `Config/AINpcStructuredOutput*.txt`, `Config/AINpcVisualHarnessInitialPrompt.txt`, `Content/Maps/AINpcTestMap.umap`, `Plugins/AINpc/Source/AINpcCore/Private/Test/*`, `Plugins/AINpc/Source/AINpcCore/Public/Test/*`, `Source/VerifierHost.Target.cs`, `VerifierHostAutomation.uproject`, this OpenSpec change directory, and the focused verification reports needed for review.
- 2026-05-20 final staging exclude list: do not stage `Config/AINpcLocalProvider.json`, `Saved/`, `Intermediate/`, `Binaries/`, `Plugins/AINpc/Binaries/`, `Plugins/AINpc/Intermediate/`, root `verification/` unless intentionally chosen as a handoff artifact, or any generated logs/caches. `git check-ignore -v Config\AINpcLocalProvider.json` reports `.gitignore:11:/Config/AINpcLocalProvider.json`; it contains local provider credentials and must stay local-only.
- 2026-05-20 final dirty-tree audit: `git status --short --ignored -- Config\AINpcLocalProvider.json openspec verification Saved Intermediate Binaries Plugins\AINpc\Binaries Plugins\AINpc\Intermediate` showed `Config/AINpcLocalProvider.json`, generated Unreal output directories, and `openspec/` as ignored, plus root `verification/` as untracked. This does not invalidate functional acceptance, but it is a submission boundary: staging must be explicit and reviewed before commit/archive.
- 2026-05-20 final shared-engine boundary note: cold review found the shared engine checkout at `G:\UE5\UnrealEngine` is not globally clean under `Engine/Source`. `git -C G:\UE5\UnrealEngine status --short -- Engine/Source` reports four tracked SDL batch files modified: `Engine/Source/ThirdParty/SDL3/SDL-gui-backend/android-project/gradlew.bat`, `Engine/Source/ThirdParty/SDL3/SDL-gui-backend/src/render/direct3d/compile_shaders.bat`, `Engine/Source/ThirdParty/SDL3/SDL-gui-backend/src/render/direct3d11/compile_shaders.bat`, and `Engine/Source/ThirdParty/SDL3/SDL-gui-backend/src/render/direct3d12/compile_shaders.bat`; `git -C G:\UE5\UnrealEngine diff --stat -- <those files>` reports `4 files changed, 116 insertions(+), 116 deletions(-)`. These files are outside `G:\UE5\AI-NPC-Plugin`, were not edited or staged by this cleanup, and must remain excluded from this subtask's submission. Therefore the closeout claim is scoped: this AI-NPC cleanup does not own or require engine-source modifications, but the shared engine workspace itself has unrelated dirty state that must not be attributed to or submitted with this subtask.
